<%= form_with(model: enrolment) do |form| %>
  <% if enrolment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrolment.errors.count, "error") %> prohibited this enrolment from being saved:</h2>

      <ul>
        <% enrolment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.collection_select :course_id, Course.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.collection_select :student_id, Student.all, :id, :first_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
